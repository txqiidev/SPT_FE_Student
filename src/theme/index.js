import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#F8F8F8", contrastText: "#313639" },
  secondary: { main: "#fbe40d", contrastText: "#000000" },
};
const themeName = "Alabaster Lemon Goat";

export default createMuiTheme({ palette, themeName });
